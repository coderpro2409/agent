{
  "name": "email-agent-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "python frontend_bundle.py dist",
    "test": "python -m unittest discover -s test -p \"test_*.py\""
  }
}
